#include <stdio.h>

int main() {
    int total;
    int counter = 0;

    scanf("%d", &total;

    for ( int rows = 1; rows <= total; rows++, counter++ ) {
        for ( int cols = 1; cols < row; cols++, counter++ ) {
            printf("%d ", counter);
        }
        printf("%d\n", counter);
    }

    return 0;
}
