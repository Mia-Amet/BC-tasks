#include <stdio.h>

int main() {
    int total;

    scanf("%d", &total;

    for ( int rows = total; rows > 0; rows-- ) {
        for ( int cols = 1; cols < row; cols++ ) {
            printf("%d ", cols);
        }
        printf("%d\n", rows);
    }

    return 0;
}
